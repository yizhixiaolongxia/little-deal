# 复制成 backend/.env 再填自己的值。真正的 .env 已被 .gitignore 排除。
#
# 注意：DB_PASSWORD 不能留空。config.py 会把它拼进连接串，
# 空密码时 aiomysql 报的是 "Access denied for user 'root'@'localhost'
# (using password: NO)"，看起来像账号错了，其实是密码没读到。

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=fundscope
