# ── 凭据：绝对不能进仓库 ──────────────────────────────────────────────
# .env 里是数据库账号密码。仓库是公开的，推上去一次就等于永久泄露
# （GitHub 有代码搜索，删了也可能留在缓存和 fork 里）。
# 要跑这个项目的人照 backend/.env.example 自己建一份。
.env
backend/.env
*.env
!*.env.example

# ── 依赖：装出来的，不是写出来的 ──────────────────────────────────────
# 这两个目录占了项目 441M 里的 435M。requirements.txt 和 package-lock.json
# 已经把版本钉死了，克隆的人重新装就行。
backend/venv/
venv/
frontend/node_modules/
node_modules/

# ── 构建产物与缓存 ────────────────────────────────────────────────────
frontend/build/
build/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# ── 运行时状态与个人数据 ──────────────────────────────────────────────
# briefs/ 是每天定时任务生成的决策简报，里面有账户总资产、每只基金的持仓
# 市值和浮亏明细。这是个人财务数据，不该跟着代码走。
# 删掉也不影响功能：daily_brief.py 明天照样生成。
briefs/

# scheduler.py 用来记「上次同步/上次生成简报」的时间戳，纯本地状态。
# 进了仓库会导致每台机器互相覆盖对方的执行记录。
.sync_stamp.json

*.log

# ── 系统与编辑器 ──────────────────────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.swp

# CRA 脚手架残留的嵌套 git 仓库备份（只含一个模板初始提交）。
# 它让 git 把 frontend/ 当子模块，前端源码一个都进不去，所以移出来了。
.cra-git-backup/
